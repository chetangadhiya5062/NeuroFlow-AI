# Infrastructure Relational Database Package (`backend/infrastructure/database/`)

## Purpose
Contains ORM models, database migrations, connection pool management, and SQL repositories implementing core data persistence ports.
