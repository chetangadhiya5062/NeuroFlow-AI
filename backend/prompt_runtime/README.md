# Prompt Runtime Package (`backend/prompt_runtime/`)

## Purpose
Manages prompt template compilation, versioning, context assembly, multi-block construction, and safety policy injection.

## Architecture Specification
Defined in `docs/architecture/prompt-runtime.md` and `docs/adr/ADR-012-prompt-runtime.md`.
