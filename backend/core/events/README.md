# Layer 0 Core Domain Events (`backend/core/events/`)

## Purpose
Defines domain event schemas emitted across the `Internal Event Bus` (e.g., `TaskCompletedEvent`, `WorkflowStartedEvent`, `AgentReasoningStepEvent`, `DocumentIngestedEvent`).
