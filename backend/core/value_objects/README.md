# Layer 0 Core Value Objects (`backend/core/value_objects/`)

## Purpose
Contains immutable, identity-less value objects representing domain measures, IDs, status states, and attributes (e.g., `TenantId`, `WorkflowExecutionId`, `EmbeddingVector`, `TokenBudget`).
