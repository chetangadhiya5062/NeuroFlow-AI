# LLM Gateway Package (`backend/llm_gateway/`)

## Purpose
Provides unified model routing, provider fallback, rate limiting, token budget management, and response normalization across heterogeneous LLM providers.
