# Workflow Engine Package (`backend/workflow_engine/`)

## Purpose
Orchestrates DAG-based execution workflows, state machines, parallel task dispatching, checkpointing, and Saga compensation rollbacks.

## Architecture Specification
Defined in `docs/architecture/workflow-engine.md` and `docs/adr/ADR-008-workflow-engine.md`.
