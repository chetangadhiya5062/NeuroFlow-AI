# Integration Test Suite (`tests/integration/`)

## Purpose
Contains integration tests verifying infrastructure adapters against real external services (PostgreSQL, Redis, Qdrant, Neo4j) managed via Docker Compose.
