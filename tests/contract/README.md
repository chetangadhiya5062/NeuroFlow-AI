# Contract Test Suite (`tests/contract/`)

## Purpose
Contains contract conformance tests verifying that every concrete infrastructure adapter completely satisfies its abstract port contract (`IXxxPort`).
