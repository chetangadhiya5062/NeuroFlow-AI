# Plugin SDK Package (`plugins/sdk/`)

## Purpose
Provides the official Plugin SDK (`NeuroFlowPluginContext`, base plugin interfaces, hook lifecycle definitions, and security sandboxing constraints) for extending NeuroFlow AI with domain intelligence.
