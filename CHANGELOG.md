# Changelog

All notable changes to the **NeuroFlow AI** platform will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initialized production-grade modular repository architecture.
- Established Clean Architecture layer contracts for backend (API, core, config, services, plugins, AI, RAG, agents, workflows, database, infrastructure, tests).
- Established Next.js App Router compatible structure for frontend (`app/`, `components/`, `features/`, `lib/`, `hooks/`, `styles/`, `types/`, `public/`).
- Added technical documentation hierarchy under `docs/` (`architecture/`, `adr/`, `api/`, `diagrams/`, `decisions/`).
- Added workspace settings template under `.vscode/`.
- Configured open-source governance guidelines (`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`).
